/**
 * Indicates the different types of main pages
 * available on the website
 */
export enum NavLocation {
   Home,
   Projects,
   Resume
}

export const PROJECT_URL = "/projects";
export const RESUME_URL = "/resume";